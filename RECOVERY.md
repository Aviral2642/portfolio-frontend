# Recovery and cleanup

September 11, rejected video removal: the prepared video, poster, build copies,
all related captured frames and review scripts were moved to macOS Trash at the
owner's request. The video component, styles, preparation script and tests were
removed. A temporary name introduction keeps the rest of the portfolio usable.
The pre-video source archive below contains the earlier workflow, not this video.

September 11 film replacement: the superseded AI-workflow hero source, its tests,
page integration and WORKFLOW.md are saved in
`../portfolio-backups/pre-film-hero-20260911.tar.gz`.
The six unused workflow implementation/test files were removed after archive
verification. Only the hero import and render changed in DesignerPortfolio.js;
the rest of that file matched the archived copy exactly. The supplied film in
Downloads was not modified. No large image sequence or dependency was added.

The source, assets and documentation before the designer redesign (including
uncommitted source, excluding dependencies, .git and generated reviews) are archived at:

`/Users/aviralsrivastava/projects/portfolio-backups/pre-designer-source-20260906.tar.gz`

SHA-256: `fc3aae5d9ad282cf3e45462fb60a403aea3ac823382465578ef736f47f2f0b20`

Storage cleanup: old76MB archive replaced with5.2MB source archive; all196
non-review files retained and file lists compared. Only historical output,
.impeccable and .playwright-cli artifacts were excluded. The superseded archive,
duplicate captures and rebuildable webpack/Babel caches were moved to macOS Trash,
not permanently erased. Empty those items in Trash to reclaim physical disk space.
Current assets, local build and current review gallery remain. Downloads untouched.

Before the full-frame correction: ../portfolio-backups/pre-full-frame-workflow-20260906.tar.gz.

Archive contents were verified before deletion. Extract to a NEW empty directory
to inspect/recover files; do not overwrite the current checkout.

Removed: rejected sculpture/sequence/world/observatory source, city datasets,
scene stills, storyboard pages/scripts, and obsolete screenshots and recordings.
Resume, original data modules, archive and backend contract retained. No push/deploy.
# September 6 refinement checkpoint

Before the workflow opening: ../portfolio-backups/pre-workflow-20260906.tar.gz.
Contains the previous designer source, content, social image and relevant notes.
The Langflow evidence block and all main JSX from recognition onward were compared
against this archive after implementation and are unchanged.

Before replacing the AS mark and portrait, the designer source and editorial
assets were saved in ../portfolio-backups/designer-before-depth-20260906.tar.gz.
The removed public/editorial/aviral-portrait.png is recoverable from that archive.
User-supplied originals in Downloads were not edited or deleted.
